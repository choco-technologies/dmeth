#ifndef DMETH_STM32F7_REGS_H
#define DMETH_STM32F7_REGS_H

#include <stdint.h>

/* ======================================================================
 *               STM32F7 Specific Ethernet Definitions
 *
 *   The Ethernet MAC/DMA IP block itself is identical to STM32F4's (see
 *   stm32_common_regs.h) - this file is reserved for values that do turn
 *   out to differ once more STM32F7 parts are validated (e.g. a part-
 *   specific HSI/clock default).
 * ====================================================================== */

#endif // DMETH_STM32F7_REGS_H
